import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: '#2d5b4f',
    },
    textToTranslate: {
        height: 100,
        fontSize: 20,
    },
    resultText: {
        height: 200,
        fontSize: 20,
    },
    footer: {
        marginTop: 50,
        marginBottom: 40,
        textAlign: "center"
    },
});
